# Crossmint backend (server-side) config
CROSSMINT_API_KEY=
# Use the staging base URL from Crossmint docs
CROSSMINT_BASE_URL=https://staging.crossmint.com/api/2022-06-09
# We’re Solana-only
CHAIN=solana

# Server config
PORT=4000
LOG_LEVEL=info

# CORS (adjust to your local Next.js and Vercel preview origins)
CORS_ORIGIN=http://localhost:3000

# Optional paths for local logging/records
LOG_FILE=server/logs/app.log
RECORDS_FILE=server/records/operations.jsonl